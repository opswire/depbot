# Tiltfile for local dev
docker_build('myorg/frontend:0.1.0', './frontend')
docker_build("myorg/backend:0.2.0", "./backend")
custom_build('gcr.io/myproj/api:3.0.0', 'echo ok', deps=['./api'])

# Skipped, comment:
# docker_build('commented/out:9.9.9', '.')

# Bazel-style, image without tag — skipped:
container_pull(
    name = "alpine",
    image = "docker.io/library/alpine",
    tag = "3.18",
)

print("hello world")
