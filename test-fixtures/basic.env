# Application
APP_PORT=8080
NODE_ENV=production
TIMEOUT=30s

# Images
APP_IMAGE=myorg/app:3.1.0
DOCKER_BASE=alpine:3.19
export NGINX_IMAGE="nginx:1.25.0"
DB_PASSWORD="not-an-image"

# Non-semver tag, skipped:
PROXY_IMAGE=traefik:latest

# Templated, skipped:
DEV_IMAGE=${BASE}:${TAG}
