# project.toml for buildpacks
[_]
schema-version = "0.2"

[io.buildpacks.builder]
image = "paketobuildpacks/builder-jammy-base:0.4.0"

# Comment with image:
# image = "fake/skipped:0.0.0"

[tool.experimental]
image = "ghcr.io/example/tool:2.1.0"

[tool.non_semver]
image = "ubuntu:latest"
